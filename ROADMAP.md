# Roadmap NeuroLumen

```
Build 0.1  — Alpha Foundation (Genesis)          ✅ Concluída
Build 0.2  — Architecture + Skeleton             ✅ Concluída
Build 0.3  — Design System completo              ✅ Concluída
Build 0.4  — HTML estruturado (sem JS)           ✅ Concluída
Build 0.5  — CSS (tokens, temas, responsividade)
Build 0.6  — JavaScript modular
Build 0.7  — React / Next.js
Build 0.8  — Backend (FastAPI + estrutura)
Build 0.9  — IA + BioData inicial
Build 1.0  — Primeira publicação pública
```

### Build 0.4 — HTML ✅
`frontend/index.html` semântico: Header → Hero → Visão → Experiência → Evidências → Galeria → Footer. Sem JavaScript.

### Build 0.5 — CSS (próxima)
Consumir `design/tokens.css`. Arquivos: `variables.css`, `style.css`, `animations.css`, `responsive.css`.

### Build 0.6 — JavaScript
Módulos: `main.js`, `navigation.js`, `layers.js`, `animation.js`, etc.

### Builds 0.7–1.0
React → Backend → IA + BioData → Publicação.
