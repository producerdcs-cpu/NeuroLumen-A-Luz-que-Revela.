# Roadmap NeuroLumen

```
Build 0.1  — Alpha Foundation                    ✅
Build 0.2  — Architecture + Skeleton             ✅
Build 0.3  — Design System completo              ✅
Build 0.4  — HTML estruturado                    ✅
Build 0.5  — CSS + GitHub Pages                  ✅
Build 0.6  — JavaScript modular                  ✅
Build 0.7-A — Partículas + rede luminosa         ✅
Build 0.7-B — React / Next.js                    ✅
Build 0.8  — Backend
Build 0.9  — IA + BioData
Build 1.0  — Publicação pública
```

### Build 0.7-A ✅
Canvas de partículas (`frontend/js/light.js` e port em Next).

### Build 0.7-B ✅
App Next.js 14 (App Router) em `web/`:
- export estático (`output: 'export'`)
- `basePath` para GitHub Pages
- componentes: SiteHeader, LightCanvas, Footer
- workflow Pages: `npm run build` → `web/out`

### Local
```bash
cd web && npm install && npm run dev
```

### Esboço de conteúdo
Leonardo → Michelangelo → Einstein → … → Enoque
(espectroscopia de evidências da luz).
